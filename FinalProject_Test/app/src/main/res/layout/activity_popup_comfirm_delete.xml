<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/popup_confirm_delete_bg">

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tvPopupTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/baloo2_medium"
        android:gravity="center"
        android:paddingBottom="8dp"
        android:text="Xác nhận xóa"
        android:textColor="#000"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Nội dung -->
    <TextView
        android:id="@+id/tvPopupMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/baloo2_bold"
        android:gravity="center"
        android:paddingBottom="16dp"
        android:text="Bạn có chắc chắn muốn xóa mục này?"
        android:textSize="17sp" />

    <!-- Nút OK và Hủy -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/btnPopupCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Hủy"
            android:fontFamily="@font/baloo2_bold"
            android:textColor="#FFFFFF"
            android:background="@drawable/btn_cancel"
            android:layout_marginEnd="20dp" />

        <Button
            android:id="@+id/btnPopupConfirm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:fontFamily="@font/baloo2_bold"
            android:text="OK"
            android:background="@drawable/btn_confirm"/>
    </LinearLayout>
</LinearLayout>
